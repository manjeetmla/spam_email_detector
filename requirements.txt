# Requirements for running `app.py` (designed for Python 3.11)
streamlit
nltk
# Pin scikit-learn to match the version used when the model was pickled
scikit-learn==1.2.2
joblib
# Pin numpy <2.0 to avoid binary incompatibility with compiled extensions
numpy==1.24.4
# Use pandas/scipy versions compatible with numpy 1.24
pandas==1.5.3
scipy==1.10.1